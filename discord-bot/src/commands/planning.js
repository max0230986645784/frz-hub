import { SlashCommandBuilder, EmbedBuilder } from "discord.js";
import { admin, getGuild, saveGuild, success } from "./_helpers.js";
export const data = admin(new SlashCommandBuilder().setName("stream-planning").setDescription("Gérer le planning des streams").addSubcommand((s) => s.setName("add").setDescription("Ajouter un stream").addStringOption((o) => o.setName("jour").setDescription("Jour").setRequired(true)).addStringOption((o) => o.setName("heure").setDescription("Heure").setRequired(true)).addStringOption((o) => o.setName("titre").setDescription("Titre").setRequired(true))).addSubcommand((s) => s.setName("remove").setDescription("Retirer un stream").addIntegerOption((o) => o.setName("index").setDescription("Index").setRequired(true))).addSubcommand((s) => s.setName("list").setDescription("Afficher le planning")));
export async function execute(i) { const cfg = await getGuild(i.guildId); cfg.planning ??= []; const sub = i.options.getSubcommand(); if (sub === "add") cfg.planning.push({ jour: i.options.getString("jour"), heure: i.options.getString("heure"), titre: i.options.getString("titre") }); if (sub === "remove") cfg.planning.splice(i.options.getInteger("index") - 1, 1); await saveGuild(i.guildId, cfg); if (sub === "list") return i.reply({ embeds: [new EmbedBuilder().setTitle("📅 Planning").setDescription(cfg.planning.map((x) => `**${x.jour} ${x.heure}** — ${x.titre}`).join("\n") || "Planning vide.")] }); return i.reply({ embeds: [success("Planning mis à jour.")] }); }
