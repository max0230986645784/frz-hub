import { SlashCommandBuilder } from "discord.js";
import { admin, getGuild, saveGuild, success } from "./_helpers.js";
function command(name, description, section) { return { data: admin(new SlashCommandBuilder().setName(name).setDescription(description).addSubcommand((s) => s.setName("config").setDescription("Configurer").addChannelOption((o) => o.setName("salon").setDescription("Salon").setRequired(true)).addStringOption((o) => o.setName("message").setDescription("Message").setRequired(true))).addSubcommand((s) => s.setName("test").setDescription("Tester le message"))), async execute(i) { const cfg = await getGuild(i.guildId); if (i.options.getSubcommand() === "test") return i.reply({ content: cfg[section].message }); cfg[section] = { ...cfg[section], enabled: true, channel: i.options.getChannel("salon").id, message: i.options.getString("message") }; await saveGuild(i.guildId, cfg); return i.reply({ embeds: [success("Configuration enregistrée.")], ephemeral: true }); } }; }
export const data = command("bienvenue", "Configurer les messages de bienvenue", "welcome").data;
export async function execute(i) { const cfg = await getGuild(i.guildId); if (i.options.getSubcommand() === "test") return i.reply({ content: cfg.welcome.message }); cfg.welcome = { ...cfg.welcome, enabled: true, channel: i.options.getChannel("salon").id, message: i.options.getString("message") }; await saveGuild(i.guildId, cfg); return i.reply({ embeds: [success("Bienvenue configurée.")], ephemeral: true }); }
